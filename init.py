#Initilizes data-compiler and provides user query to application

team_num = input("Team Number Search:")

user_req = input("Raw or Compiled? (Data)")
user_req = str.lower(user_req)

if user_req = compiled
    #import score_avg, ect.
    #print("score_avg, ect.") (but prettier) 

